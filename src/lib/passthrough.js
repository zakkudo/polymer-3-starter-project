/**
 * Utility method to passthrough the first argument
 * of a function
 * @param {*} first - The first arugment
 * @return {*} The first argument
 * @module lib/passthrough
 */
export default function passthrough(first) {
    return first;
}
