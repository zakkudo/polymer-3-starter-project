
/**
 * Generates a deferred promise that can be resolved or rejected externally.
 * @typedef {Object} DeferredPromise
 * @property {Promise} promise - The unresolved promise.
 * @property {Function} resolve - Function that resolves the promise.
 * @property {Function} reject - Function that rejects the promise.
 */


